node ('local_ubuntu'){  
    //def app
    stage('Cloning Git') {
        /* Let's make sure we have the repository cloned to our workspace */
       checkout scm
    }  
    stage('build') {
        /* Let's make sure we have the repository cloned to our workspace */
       echo "build phase"
    } 
}
